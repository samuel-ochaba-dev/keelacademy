Containerized Docker environments ensure tests run reproducibly with 2 CPU and 2GB RAM limits without local environment drift.
Hard monthly provider spend limits protect against runaway costs from recursive retry bugs.
API keys must always be stored in environment variables and excluded from git via .gitignore.
